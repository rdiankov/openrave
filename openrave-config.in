#!/bin/sh

prefix=@CMAKE_INSTALL_PREFIX@
exec_prefix=@CMAKE_INSTALL_PREFIX@
exec_prefix_set=no

usage="\
Usage: openrave-config [--prefix[=DIR]] [--exec-prefix[=DIR]] [--version] [--cflags] [--libs] [--libs-only-l] [--libs-only-L] [--cflags-only-I] [--shared-libs] [--python-dir] [--octave-dir] [--matlab-dir] [--usage | --help]"

if test $# -eq 0; then
      exit 1
fi

while test $# -gt 0; do
  case "$1" in
  -*=*) optarg=`echo "$1" | sed 's/[-_a-zA-Z0-9]*=//'` ;;
  *) optarg= ;;
  esac

  case $1 in
    --prefix=*)
      prefix=$optarg
      if test $exec_prefix_set = no ; then
        exec_prefix=$optarg
      fi
      ;;
    --prefix)
      echo $prefix
      ;;
    --exec-prefix=*)
      exec_prefix=$optarg
      exec_prefix_set=yes
      ;;
    --exec-prefix)
      echo $exec_prefix
      ;;
    --version)
      echo @OPENRAVE_VERSION@
      ;;
    --cflags)
      echo  -I@CMAKE_INSTALL_PREFIX@/include @OPENRAVE_BOOST_INCLUDE_DIRS@
      ;;
    --cppflags)
      echo  -I@CMAKE_INSTALL_PREFIX@/include @OPENRAVE_BOOST_INCLUDE_DIRS@
      ;;
    --cflags-only-I)
      echo -I@CMAKE_INSTALL_PREFIX@/include @OPENRAVE_BOOST_INCLUDE_DIRS@
      ;;
    --libs)
      echo -L@CMAKE_INSTALL_PREFIX@/lib -lopenrave @OPENRAVE_BOOST_LIB_DIRS@ -l@Boost_THREAD_LIBRARY@
      ;;
    --libs-only-l)
      echo -lopenrave -l@Boost_THREAD_LIBRARY@
      ;;
    --libs-only-L)
      echo -L@CMAKE_INSTALL_PREFIX@/lib @OPENRAVE_BOOST_LIB_DIRS@
      ;;
    --shared-libs)
      echo -L@CMAKE_INSTALL_PREFIX@/lib -lopenrave.so @OPENRAVE_BOOST_LIB_DIRS@ -l@Boost_THREAD_LIBRARY@
      ;;
    --python-dir)
      echo @OPENRAVE_PYTHON_INSTALL_DIR@
      ;;
    --octave-dir)
      echo @OPENRAVE_OCTAVE_INSTALL_DIR@
      ;;
    --matlab-dir)
      echo @OPENRAVE_MATLAB_INSTALL_DIR@
      ;;
    --usage)
      echo "${usage}" 1>&2
      exit 1
      ;;
    --help)
      echo "${usage}" 1>&2
      exit 1
      ;;
    *)
      echo "${usage}" 1>&2
      exit 1
      ;;
  esac
  shift
done
