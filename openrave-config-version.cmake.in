set( PACKAGE_VERSION "@OPENRAVE_VERSION@" )
if( PACKAGE_FIND_VERSION )
  if( "${PACKAGE_FIND_VERSION}" EQUAL "@OPENRAVE_VERSION@")
    set(PACKAGE_VERSION_EXACT 1)
  endif()
  if( "${PACKAGE_FIND_VERSION_MAJOR}" EQUAL "@OPENRAVE_VERSION_MAJOR@" )
    if( "${PACKAGE_FIND_VERSION_MINOR}" LESS 3 )
      set(PACKAGE_VERSION_UNSUITABLE 1)
    elseif( "${PACKAGE_FIND_VERSION_MINOR}" EQUAL 3 )
      set(PACKAGE_VERSION_COMPATIBLE 1)
    endif()
  endif()
else( PACKAGE_FIND_VERSION )
  set(PACKAGE_VERSION_COMPATIBLE 1)
endif( PACKAGE_FIND_VERSION )
