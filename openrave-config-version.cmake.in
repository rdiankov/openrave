set( PACKAGE_VERSION "@OPENRAVE_VERSION@" )
if( "${PACKAGE_FIND_VERSION}" VERSION_EQUAL "@OPENRAVE_VERSION@")
  set(PACKAGE_VERSION_EXACT 1)
  set(PACKAGE_VERSION_COMPATIBLE 1)
endif()

if( "${PACKAGE_FIND_VERSION}" VERSION_LESS "@OPENRAVE_VERSION@")
  set(PACKAGE_VERSION_COMPATIBLE 1)
endif()
